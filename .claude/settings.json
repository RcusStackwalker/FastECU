{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; if [ \"$(basename -- \"$f\" 2>/dev/null)\" = \"BUILD.bazel\" ]; then bazel run --config=release //bazel/compile_commands:refresh >/tmp/claude-compile-commands-refresh.log 2>&1; fi; } 2>/dev/null || true",
            "async": true,
            "statusMessage": "Refreshing compile_commands.json (BUILD.bazel changed)..."
          }
        ]
      }
    ]
  }
}
