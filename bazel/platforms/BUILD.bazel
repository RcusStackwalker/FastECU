package(default_visibility = ["//visibility:public"])

# x86_smoke_test.cc is compiled two ways:
# - As a genuine x64 binary by //:pe_bitness_x64_fixture (this repo's Bazel
#   setup has no x86 Windows C++ toolchain, so there's no Bazel x86 variant
#   any more -- see scripts/compile-x86-bridge-artifacts.ps1, which compiles it
#   directly via cl.exe for the x86 fixture instead).
exports_files(["x86_smoke_test.cc"])
