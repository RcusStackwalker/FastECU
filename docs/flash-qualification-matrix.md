<!-- docs/flash-qualification-matrix.md -->
# Flash Qualification Matrix

## Schema

| Column | Meaning |
|---|---|
| `family_id` | Stable code identifier, one row per selectable operation family/transport variant |
| `scope` | ECU, TCU, EEPROM, JTAG, BDM, or bootmode |
| `transport` | K-Line, raw CAN, ISO-15765, SSM, JTAG, or BDM |
| `operations` | Actual supported `read`, `test_write`, `write` set; no inferred capability |
| `portable` | `yes` only when plan + executor are in enforced portable closure |
| `automated_evidence` | Golden/state-machine test labels and last qualifying revision |
| `hardware_status` | `unqualified`, `experimental`, or `proven` |
| `hardware_evidence` | Date, ECU/TCU/adapter identity, operation, operator/report reference; `—` if absent |
| `notes` | Known compatibility quirks, unsupported branches, and safety limitations |

## Rules

- Seed one row for every current backend flash/eeprom/jtag/bdm/bootmode family, not only the proving pair. No family silently disappears from migration scope.
- The proving pair has `portable=yes`, test labels, and `hardware_status=experimental`; it is never marked proven from unit tests.
- Unmigrated rows have `portable=no` and `hardware_status=unqualified` unless concrete historical hardware evidence is entered with a reference.
- `proven` requires a real read comparison and, where the family supports it, test-write/write/verify/recovery checks on named hardware. A future change records evidence; it does not infer proof from similarity to another family.
- Matrix status is documentation only. It does not drive UI badges, dialogs, or runtime feature flags in step 5.

## Families

<!-- One row per family, grouped by scope then stable family ID. Populate
     every column from the source enumeration performed at task start; do
     not leave any row's operations/transport column as a guess. -->

| family_id | scope | transport | operations | portable | automated_evidence | hardware_status | hardware_evidence | notes |
|---|---|---|---|---|---|---|---|---|
| DensoSh705xEepromKline | EEPROM | K-Line | read | yes | `denso_sh705x_eeprom_common_test`, `denso_sh705x_eeprom_kline_executor_test` @ step 5c | experimental | — | write/test_write unsupported: legacy write call was already commented out; portable builder returns Unsupported rather than legitimizing a no-op write |
| DensoSh705xEepromCan | EEPROM | ISO-15765 | read | yes | `denso_sh705x_eeprom_common_test`, `denso_sh705x_eeprom_can_executor_test` @ step 5c | experimental | — | same write/test_write gap as the K-Line sibling; `_ecutek_racerom_alt` RAM-location read fixes a pre-existing legacy bug (read from `received`, not the unpopulated `response`) — see executor comment |
| FlashEcuMitsuM32rCan | ECU | ISO-15765 | read, write | yes | `mitsu_colt_m32r_can_plan_test`, `mitsu_colt_m32r_can_executor_test`, `test_flash_ecu_mitsu_m32r_can_dialog`, `colt_capacity_protocols_config_test`, `checksum_ecu_mitsu_m32r_can_test`, `dispatch_test` @ Colt capacity-protocol feature | experimental | — | Portable `MitsuColtM32rCanPlan`/`MitsuColtM32rCanExecutor` (`src/backend/flash/ecu/`) serves four exact IDs. `mitsu_ecu_m32r_can` and `mitsu_ecu_m32r_can_vendor_ext` select 384 KiB: reads cover `0x00000`-`0x60000`, and writes require an exact 384 KiB image and cover `0x08000`-`0x60000`. `mitsu_ecu_m32r_can_512kb` and `mitsu_ecu_m32r_can_vendor_ext_512kb` select 512 KiB: reads cover `0x00000`-`0x80000`, and writes require an exact 512 KiB image and cover `0x08000`-`0x80000`; the `0x60000`-`0x80000` top region uses the retained conditional bootstrap and is skipped when already equal. All writes preserve full-file absolute offsets and protect `0x00000`-`0x08000`; the default 384 KiB protocols never run the top-region bootstrap. The two vendor IDs use the vendor authorization handshake at their respective capacities. `test_write` remains `Unsupported`. Capacity-specific confirmations are collected before execution, and the warning calls out post-erase cancellation/recovery risk. ROM checksum correction is wired for all four IDs via `ChecksumEcuMitsuM32rCan` (`protocols.cfg` `<checksum>yes</checksum>`): the sweep length comes from the ROM's own area-code byte at `0x3FFCB`, and the balance word at `0x3FFC0` is rewritten to make the ECU's flash CRC checker finalize to `0x5AA55AA5`. `checksum_ecu_mitsu_m32r_can_test` verifies corrected images against a transcription of the ECU's own checker rather than the module's arithmetic, covering both its passes. Correction is confined to `0x3FFC0`-`0x3FFC3`, inside the written window and outside the `0x56000`-`0x5F0D0` second-pass pages. No hardware evidence yet: step 10 of the checklist gates it, and requires the negative control (an uncorrected image must raise the ROM checksum DTC). A [bench-qualification checklist](colt_czt_47110032_can_bench_checklist.md) separately gates both capacities before real-vehicle use, so `hardware_status` remains `experimental`. Exchanges run through the shared UDS layer (`UdsClient` over `CanFlashUdsChannel`): NRC 0x78 responsePending is absorbed by re-reading, up to ten repeats, and the reply CAN id is validated. The pending-retry path is unexercised on hardware — see the [bench checklist](colt_czt_47110032_can_bench_checklist.md). |
| FlashEcuSubaruDenso1N83M_1_5MCan | ECU | ISO-15765 | read, write | yes | `subaru_denso_1n83m_1_5m_can_plan_test`, `subaru_denso_1n83m_1_5m_can_executor_test` @ Wave 4 | experimental | — | Protocol `sub_ecu_denso_1n83m_1_5m_can`; cfg `test_write=no`. `test_write` is rejected with `Unsupported` in both the plan builder and the executor, before any transport call: legacy threaded `test_write` from `execute()` through `write_memory` into `reflash_block` and then never consulted it, so a `test_write` run performed a real erase and a real `0xB6` flash write — the rejection closes that live-write hazard rather than merely declining an unsupported mode. Both programming branches are ported: `connect_bootloader` branches on byte 7 of the `0x22 0x10 0x1D` reply (`0xFF` bench, anything else in-car), and the in-car arm additionally addresses CAN ids `0x7A2`, `0x7DF`, `0x7E1` and `0x7B0` with ten fire-and-forget exchanges whose replies legacy never inspects — transcribed with per-exchange legacy line citations and pinned byte-exactly by `InCarReadReturnsPaddedImage`. Read and write both target `fblocks_N83M_1_5MB[1]` (`0x08FAC000` + `0x173F00`), the window legacy's `read_memory` hardcodes over its own arguments; a read returns `0x184000` bytes (`0x10000` of `0xFF`, the block, then `0x100` of `0xFF`), and a write requires an image of exactly that size indexed from `fblocks[0].start` = `0x08F9C000`. `N83M_1_5MB`'s flash-table `romsize` (`0x174000`) does not equal its own `fblocks` sum (`0x184000`); nothing on either path consumes `romsize`, so the inconsistency is recorded and left untouched. Neither branch's kernel-jump wait loop can fail — legacy returns success whether or not the ECU ever acknowledges — and that is preserved. A short `0xB7` page reply silently shortens the returned dump (each page is appended as decrypted with no per-page length check, and the loop still advances by a fixed page size); a bench operator should verify a completed read is exactly `0x184000` bytes before trusting it as a full ROM. Checksum verification is also slightly more permissive than legacy: legacy required the ECU's `7F 31 78` pending answer before accepting the positive `71 01 02` result and rejected an immediate positive as a mismatch, whereas the port's shared UDS layer accepts either — same wire traffic in the passing case, and this only matters if an ECU firmware answers immediately, which legacy never accepted. Both of legacy's checksum-verify reads use `receive_timeout` = 500 ms (lines 1308 and 1329), so the port pins its `pending_timeout_ms` to 500 rather than inheriting the shared UDS layer's 3000 ms default; the four families' pairs are 500/500, 2000/2000, 2000/2000 and 500/3000, and each carries its own. No bench access; `hardware_status` is `experimental` from unit tests alone. |
| FlashEcuSubaruDenso1N83M_4MCan | ECU | ISO-15765 | read, write | yes | `subaru_denso_1n83m_4m_can_plan_test`, `subaru_denso_1n83m_4m_can_executor_test` @ Wave 4 | experimental | — | Protocol `sub_ecu_denso_1n83m_4m_can`; cfg `test_write=no`. **This family is the tolerant outlier of its cluster.** Seven response checks have their `return STATUS_ERROR` commented out in legacy (lines 305, 335, 369 in `connect_bootloader`; 876, 883, 917, 924 in `read_memory`), so a wrong or missing reply to the access-method probe, the programming-branch selector, the in-car access probe, or **either dump-setup PDU (`0x34` RequestDownload and `0x35` RequestUpload)** is logged and stepped over rather than aborting. That is preserved byte-for-byte. **Operator consequence: a dump can start against an ECU that never acknowledged the transfer window**, so a read that completes still has to be checked — a good dump is exactly `0x3E4000` bytes and its log carries no `Wrong response from ECU` or `No valid response from ECU` line before `Start reading ROM`. The four `read_memory` checks are a real behavioural divergence from the otherwise near-identical `sub_ecu_denso_1n83m_1_5m_can` and `sub_ecu_denso_sh72531_can`, which abort at all four; the three `connect_bootloader` ones match **all three** siblings' behaviour — none of them aborts at those probes either (in the 1N83M 1.5M and SH72531 sources the matching branches are lines 299-304, 327-332 and 359-364, and they log without returning; SH72543d's are the same, offset by a line at the third site) — and differ only in carrying the commented-out line. Both directions are pinned by tests: `ProceedsPastMalformedConnectAndDumpSetupResponses` and `ProceedsPastMalformedInCarProbeAndDumpSetup` here, `NegativeResponseAtDumpSetupFails` in all three strict siblings. The bench arm additionally performs a **duplicate `read_serial_data` after the jump to the on-board kernel** (legacy lines 788-791: the first frame is read and discarded, a `delay(50)` follows, and only the second seeds the wait loop). Its `sub_ecu_denso_sh72531_can` sibling does the same — see that row — and only `sub_ecu_denso_1n83m_1_5m_can` reads once; reproduced explicitly here and there. `test_write` is rejected with `Unsupported` in both the plan builder and the executor, before any transport call: legacy threaded `test_write` from `execute()` through `write_memory` into `reflash_block` and then never consulted it, so a `test_write` run performed a real erase and a real `0xB6` flash write — the rejection closes that live-write hazard. Both programming branches are ported: `connect_bootloader` branches on byte 7 of the `0x22 0x10 0x1D` reply (`0xFF` bench, anything else in-car), and the in-car arm additionally addresses CAN ids `0x7A2`, `0x7DF`, `0x7E1` and `0x7B0` with ten fire-and-forget exchanges whose replies legacy never inspects — transcribed with per-exchange legacy line citations and pinned byte-exactly by `InCarReadReturnsPaddedImage`. Read and write both target `fblocks_N83M_4MB[1]` (`0x08FAC000` + `0x3D3F00`); a read returns `0x3E4000` bytes (`0x10000` of `0xFF`, the block, then `0x100` of `0xFF`), and a write requires an image of exactly that size indexed from `fblocks[0].start` = `0x08F9C000` — `reflash_block`'s `newdata[i + blockaddr - fblocks[0].start]` composed with `write_memory` passing `&data_array[0]`, which is the absolute flash address, not an offset divergence. Unlike its 1.5M sibling, `N83M_4MB`'s flash-table `romsize` (`0x3E4000`) does equal its own `fblocks` sum. Further preserved legacy hazards: neither branch's kernel-jump wait loop can fail (legacy returns success whether or not the ECU ever acknowledges); the read stop-command loop's outcome is likewise never checked; a short `0xB7` page reply silently shortens the returned dump (each page is appended as decrypted with no per-page length check, and the loop still advances by a fixed page size), which is the second reason to verify a completed read is exactly `0x3E4000` bytes; and checksum verification is slightly more permissive than legacy — legacy required the ECU's `7F 31 78` pending answer before accepting the positive `71 01 02` result and rejected an immediate positive as a mismatch, whereas the port's shared UDS layer accepts either (same wire traffic in the passing case). Checksum-verify reproduces both of legacy's reads at their own timeouts: the first at 500 ms (line 1322) and only the re-read after the ECU's `7F 31 78` pending answer at `serial_read_extra_long_timeout` = 3000 ms (line 1341). That 500/3000 pair is this family's own, not a shared one: `sub_ecu_denso_1n83m_1_5m_can` reads 500/500 and the two SH families read 2000/2000 at the same exchange, so each carries its own checksum policy rather than inheriting the shared 3000 ms `pending_timeout_ms` default. No bench access; `hardware_status` is `experimental` from unit tests alone. |
| FlashEcuSubaruDensoMC68HC16Y5_02 | ECU | K-Line | varies by protocol variant — see notes | yes | `subaru_denso_mc68hc16y5_02_plan_test`, `subaru_denso_mc68hc16y5_02_executor_test`, `test_flash_workflow` @ Wave 2 | experimental | — | Portable routing accepts `sub_ecu_denso_mc68hc16y5_02` / `_02_ecutek` (alias `wrx02`; cfg read/test_write/write = yes/yes/yes) and retains the reachable `sub_ecu_denso_mc68hc16y5_02_tpu` quirk: its own `<mode>` is `BDM`, but it is intentionally handled as a `wrx02`-shaped K-Line session. `_04` / `_04_ecutek` are now rejected rather than silently retaining the legacy class's unreachable-by-cfg route (cfg read = `n/a`/`no`, test_write=no, write=no). `_02_bdm` stays with the distinct BDM family below. Historical serial-drain evidence remains insufficient for hardware qualification; no hardware qualification is claimed. |
| FlashEcuSubaruDensoSH7055_02 | ECU | K-Line, raw CAN | read, test_write, write | yes | `subaru_denso_sh7055_02_plan_test`, `subaru_denso_sh7055_02_executor_test`, `test_flash_workflow` @ Wave 2 | experimental | — | Protocols `sub_ecu_denso_sh7055_02` and `_02_ecutek` (alias `fxt02`; cfg read/test_write/write = yes/yes/yes for both). `protocols.cfg` claims `K-Line,CAN`, but both the legacy operation and portable plan/executor are K-Line-only (`set_is_can_connection(false)`; no CAN I/O), so CAN is not implemented on this path. Portable planning always surfaces `CycleIgnition` upfront before execution; legacy showed the same confirmation only mid-session after its kernel-alive check failed. Historical bench-timing evidence in commit `18fb9c83d53721cafde462e367e8a93087454b40` lacks adapter identity, ECU serial, and a pass/fail report, so no hardware qualification is claimed. |
| FlashEcuSubaruDensoSH7058Can | ECU | ISO-15765 | read, test_write, write | yes | `subaru_denso_sh7058_can_plan_test`, `subaru_denso_sh7058_can_executor_test`, `test_flash_workflow` @ Task 7 | experimental | — | Exact portable routes only: `sub_ecu_denso_sh7058_can`, `_ecutek`, `_ecutek_racerom`, `_ecutek_racerom_alt`, and `_cobb`; all five preserve the enum-selected security vector, SH7058/0x100000 geometry, ISO request/response IDs 0x7E0/0x7E8 at 500000, and read/test-write/write capabilities. Before the caller-owned initial configure/open, the executor performs the legacy connection reset with no family delay; reset failure and cancellation stop setup. The executor keeps proprietary BEEF kernel traffic on the explicit channel while UDS handles only strict positive-response exchanges; initial kernel-ID probe mismatches are tolerated as in legacy, kernel start accepts both service-only `0x71` and a full routine echo, and the post-start kernel-ID verification remains strict. **Task 7 four-artifact deterministic-log correction:** (1) oracle revision `59f4e442` samples an unstarted `QElapsedTimer` in `read_mem` (lines 877/974) and formats uninitialized `curspeed`/`tleft` before assignment in `flash_block` (lines 1535-1536/1651-1655); (2) fixed-clock executor tests reproduce the zero-elapsed invariant; (3) the port samples the monotonic clock around each read page/write buffer and clamps elapsed time to 1 ms while retaining legacy formulas/format; (4) exact corrected `1024000 B/s` and `512000 B/s` records are asserted in the proving pair. **Wave 5 factoring closeout:** stock seed, standard index, and encrypt constants are shared only after byte-exact comparison; normal-ROM BEEF read payloads remain raw, and no Wave 5 ROM read consumes the common decrypt table. EcuTek/RaceRom/Cobb selection and tables remain family-local. Qualification is automated-only; no bench or vehicle hardware qualification is claimed. |
| FlashEcuSubaruDensoSH7058CanDiesel | ECU | ISO-15765 | read, test_write, write | yes | `subaru_denso_sh7058_can_diesel_plan_test`, `subaru_denso_sh7058_can_diesel_executor_test`, `test_flash_workflow` @ Wave 5 | experimental | — | Exact portable routes `sub_ecu_denso_sh7058_can_diesel` (SH7058d, 0x100000 ROM, kernel 0xFFFF4000) and `sub_ecu_denso_sh7059_can_diesel` (SH7059d, 0x180000 ROM, kernel 0xFFFEE000); aliases remain catalog-only. Both retain the family-specific initial reset → 500 ms → configure/open sequence unchanged, ISO request/response 0x7E0/0x7E8 at 500000, 16-block geometry, kernel-backed read/test-write/write, and no extra confirmations. Proprietary BEEF traffic is explicit: the initial kernel-ID probe tolerates timeout/short/malformed/wrong-ID/adapter replies, post-upload kernel-ID is strict, each B6 reply is read and discarded with only cancellation/disconnect propagated, and identity queries plus 10-03 then 10-43 fallback precede strict stock seed/key security. Test-write selects FLASH_DISABLE, BLANK_PAGE, buffer writes, and VALIDATE (0x23) and never commits (0x24). Workflow tests prove catalog resolution before Begin, deletion of the catalog/kernel after Begin, exact generation fields, and a real DesktopCan attempt/configuration. **Four-artifact dynamic-log correction:** (1) revision `59f4e442` reads an unstarted timer in `read_mem` and formats uninitialized speed/time fields before their first assignment in `flash_block`; (2) deterministic executor fixtures reproduce the zero-elapsed invariant; (3) the port samples monotonic time around every page/buffer and clamps elapsed time to 1 ms while retaining the legacy formulas and formats; (4) exact corrected log records are pinned by the executor tests. **Wave 5 factoring closeout:** stock seed, standard index, and encrypt constants are shared; the decrypt table is deliberately not consumed because this oracle returns BEEF read payloads raw. Qualification is automated-only; no bench or vehicle hardware qualification is claimed. |
| Subaru Denso SH705x DensoCAN | ECU | raw CAN + ISO-15765 proprietary BEEF (not UDS) | read, test_write, write | yes | `subaru_denso_sh705x_densocan_plan_test`, `subaru_denso_sh705x_densocan_executor_test`, `test_desktop_mixed_can_flash_transport`, `test_flash_workflow` @ Wave 5 | experimental | — | Exact portable routes only: `sub_ecu_denso_sh7055_densocan`, `sub_ecu_denso_sh7058_densocan`, `sub_ecu_denso_sh7058s_densocan`, `sub_ecu_denso_sh7058s_diesel_densocan`, and `sub_ecu_denso_sh7059_diesel_densocan`; suffixes and future DensoCAN IDs remain unrouted, while EEPROM routes retain priority. Before the caller-owned initial mixed ISO/raw configure/open, the executor performs the legacy connection reset; reset failure and cancellation stop setup. The executor starts and ends on ISO-15765 for proprietary BEEF kernel traffic, uses 29-bit raw CAN only for the bootloader transition, and does not use UDS; the existing raw/ISO transition sequence is unchanged. The initial kernel-ID probe maps no-frame, timeout, short, malformed, wrong-ID, wrong-content, and nonterminal adapter outcomes to legacy raw-bootloader fallback; cancellation and disconnection remain terminal, and post-upload kernel verification remains strict. **Fix Round 1 four-artifact dynamic-log correction:** (1) the revision-`59f4e442` oracle's `read_mem` samples an unstarted `QElapsedTimer` at lines 609-633, and `flash_block` logs `curspeed`/`tleft` before their first assignment at lines 1168-1171 and 1283-1317; (2) deterministic `FakeClock` exact-record tests reproduce the zero-elapsed invariant; (3) the portable executor samples `IClock::now_ms()` before each page/buffer loop and clamps zero elapsed time to 1 ms while retaining the legacy formula and format; (4) ordered Read/TestWrite/Write records pin corrected `1024000 B/s` and `512000 B/s` values. **Wave 5 factoring closeout:** this mixed-transport family remains excluded from the ISO-only common constants. Its normal-ROM BEEF reads, caller-image buffer writes, validation, and CRCs all use raw bytes; no DensoCAN normal-ROM payload table or helper survives. Qualification is automated-only: no bench or vehicle hardware qualification is claimed. |
| FlashEcuSubaruDensoSH705xKline | ECU | K-Line, raw CAN | varies by protocol variant — see notes | no | — | unqualified | — | Serves `sub_ecu_denso_sh7055_04` / `_04_ecutek` (yes/yes/yes) and `sub_ecu_denso_sh7058` / `_ecutek` (alias `sti05*`; yes/yes/yes), plus the Cobb-tuned variants `sub_ecu_denso_sh7055_04_cobb` and `sub_ecu_denso_sh7058_cobb`, which cfg declares read=no, test_write=yes, write=no — i.e. this same class is cfg-gated to test-write only for the Cobb variant. |
| FlashEcuSubaruDensoSH72531Can | ECU | ISO-15765 | read, write | yes | `subaru_denso_sh72531_can_plan_test`, `subaru_denso_sh72531_can_executor_test` @ Wave 4 | experimental | — | Protocol `sub_ecu_denso_sh72531_can`; cfg `test_write=no`. `test_write` is rejected with `Unsupported` in both the plan builder and the executor, before any transport call: legacy threaded `test_write` from `execute()` through `write_memory` into `reflash_block` and then never consulted it, so a `test_write` run performed a real erase and a real `0xB6` flash write — the rejection closes that live-write hazard rather than merely declining an unsupported mode. Both programming branches are ported: `connect_bootloader` branches on byte 7 of the `0x22 0x10 0x1D` reply (`0xFF` bench, anything else in-car), and the in-car arm additionally addresses CAN ids `0x7A2`, `0x7DF`, `0x7E1` and `0x7B0` with ten fire-and-forget exchanges whose replies legacy never inspects — transcribed with per-exchange legacy line citations and pinned byte-exactly by `InCarReadReturnsPaddedImage`. Note the `0x7E1` exchange sends `0x10 0x03`, not the `0x10 0x63` its 1N83M 1.5M sibling sends at the same step. Read and write both target `fblocks_SH72531[1]` (`0x00008000` + `0x00137F00`), the window legacy's `read_memory` hardcodes over its own arguments; a read returns `0x140000` bytes (`0x8000` of `0xFF`, the block, then `0x100` of `0xFF`), and a write requires an image of exactly that size indexed from `fblocks[0].start` = `0x00000000`. Neither branch's kernel-jump wait loop can fail — legacy returns success whether or not the ECU ever acknowledges — and that is preserved; the bench arm's jump additionally reads twice with legacy's `delay(50)` between the reads (legacy lines 782-785) and keeps only the second reply, which is preserved and pinned by `BenchKernelJumpDiscardsFirstReply`. Its `sub_ecu_denso_1n83m_4m_can` sibling does the same. **Operator hazards preserved by design.** (1) A short `0xB7` page reply silently shortens the returned dump: legacy performs no per-page length check and advances the sweep by a fixed `0x100` page, and the port does the same, so an operator must verify a completed read is exactly `0x140000` bytes before treating it as a good image. (2) Checksum verification is more permissive than legacy: legacy required the `7F 31 78` responsePending answer to arrive before it would accept a positive `71 01 02` result and failed the block without it, whereas the port's shared UDS layer accepts either an immediate positive answer or a pending-then-positive pair. Both of legacy's checksum-verify reads use `serial_read_timeout` = 2000 ms (lines 1310 and 1330), so the port pins its `pending_timeout_ms` to 2000 rather than inheriting the shared UDS layer's 3000 ms default. No bench access; `hardware_status` is `experimental` from unit tests alone. |
| FlashEcuSubaruDensoSH72543CanDiesel | ECU | ISO-15765 | read, write | yes | `subaru_denso_sh72543_can_diesel_plan_test`, `subaru_denso_sh72543_can_diesel_executor_test` @ Wave 4 | experimental | — | Protocol `sub_ecu_denso_sh72543_can_diesel`; cfg `test_write=no`. `test_write` is rejected with `Unsupported` in both the plan builder and the executor, before any transport call: legacy threaded `test_write` from `execute()` through `write_memory` into `reflash_block` and then never consulted it, so a `test_write` run performed a real erase and a real `0xB6` flash write — the rejection closes that live-write hazard rather than merely declining an unsupported mode. Both programming branches are ported: `connect_bootloader` branches on byte 7 of the `0x22 0x10 0x1D` reply (`0xFF` bench, anything else in-car), and the in-car arm additionally addresses CAN ids `0x7A2`, `0x7DF`, `0x7E1` and `0x7B0` with ten fire-and-forget exchanges whose replies legacy never inspects — transcribed with per-exchange legacy line citations and pinned byte-exactly by `InCarReadReturnsPaddedImage`. This family reads the ECU id with ReadDataByIdentifier `22 F1 82` → `62 F1 82` rather than the vendor `0xAA`/`0xEA` pair its three Denso ISO-15765 siblings use, and reads most exchanges with the 2000 ms `serial_read_timeout` where the siblings use the 200 ms short timeout; both are preserved per-family. Read and write both target `fblocks_SH72543d[0]` (`0x00008000` + `0x001F7F00`) — this is the wave's only single-block flash table, because the table's `0x0`–`0x8000` entry is commented out. A read returns `0x200000` bytes (`0x8000` of `0xFF`, the block, then `0x100` of `0xFF`), and a write requires an image of exactly that size, indexed from address `0x0`. Neither branch's kernel-jump wait loop can fail — legacy returns success whether or not the ECU ever acknowledges — and that is preserved. **Bench checklist — first item: confirm the write image base.** The wave-4 design flagged this family's `reflash_block` as writing a full ROM `0x8000` low, reading its `newdata[i + blockctr * blocksize]` index (legacy line 1217) on its own. Re-deriving it together with the caller — `write_memory` passes `&data_array[fdt->fblocks->start]`, i.e. the encrypted buffer already offset by `fblocks[0].start` = `0x8000` (legacy line 1129) — shows the two compose to `data_array[block_addr + i]`, the absolute flash address. The port indexes the absolute address too, so it is byte-identical to legacy here and is **not** a behavior change; `WriteTakesBytesFromTheAbsoluteAddress` pins it. Because the design doc says otherwise and this family has no bench access, an operator's first bench action must still be to read back a written ROM and confirm the bytes land at the addresses they came from — and no one should "restore" a `0x8000` shift on the strength of that design section without doing so. **Kernel fields in cfg are unused.** `protocols.cfg` declares `<kernel>ssmk_can_tp_sh72543d_euro6.bin</kernel>` and `<kernel_addr>0xFFF80000</kernel_addr>` for this protocol, but neither legacy nor the port uploads a kernel: this family talks to the ECU's on-board kernel (OBK) after a `0x10 0x42`/`0x10 0x62` session jump. The plan is kernel-free and `validate_subaru_denso_sh72543_can_diesel_plan` rejects a plan that carries one. **Operator hazards preserved by design.** (1) A short `0xB7` page reply silently shortens the returned dump: legacy performs no per-page length check and advances the sweep by a fixed `0x100` page, and the port does the same, so an operator must verify a completed read is exactly `0x200000` bytes before treating it as a good image. (2) Checksum verification is more permissive than legacy: legacy required the `7F 31 78` responsePending answer to arrive before it would accept a positive `71 01 02` result and failed the block without it (legacy lines 1329–1345), whereas the port's shared UDS layer accepts either an immediate positive answer or a pending-then-positive pair. Both of legacy's checksum-verify reads use `serial_read_timeout` = 2000 ms (lines 1328 and 1348), so the port pins its `pending_timeout_ms` to 2000 rather than inheriting the shared UDS layer's 3000 ms default. (3) The bench arm's `0x10 0x43` session check is tolerant: legacy's `return STATUS_ERROR` there is commented out (legacy line 672), so a wrong `50 43` answer is logged and the sequence continues into the seed request — preserved, and pinned by `BenchSessionMismatchIsToleratedAndContinues`. No bench access; `hardware_status` is `experimental` from unit tests alone. |
| FlashEcuSubaruHitachiM32rCan | ECU | ISO-15765 | read, write | yes | subaru_hitachi_m32r_can_plan_test, subaru_hitachi_m32r_can_executor_test @ Wave 3 | experimental | — | Protocol `sub_ecu_hitachi_m32r_can`; cfg `test_write=no`. On-car programming branch (CAN ids beyond 0x7E0/0x7E8) is out of scope for this port -- `build_subaru_hitachi_m32r_can_plan` targets the bench path only; on-car mode is not exposed by any current `protocols.cfg` entry, so this is a scope boundary, not a behavior regression. |
| FlashEcuSubaruHitachiM32rKline | ECU | K-Line | read, write | yes | `subaru_hitachi_m32r_kline_plan_test`, `subaru_hitachi_m32r_kline_executor_test`, `test_flash_workflow`, `test_flash_dialog`, `test_protocols_cfg_subaru_hitachi_m32r_kline` @ Wave 1 | experimental | — | Portable `SubaruHitachiM32rKlinePlan`/executor owns exact normal and recovery IDs with `M32R_512KB_1block`. Reads return the logical 512 KiB image using the `0x100000` wire-address bias and 38400 probe/4800 initialization fallback; recovery changes writes only. Normal writes retain active-OBK probing and full fallback, recovery retains a bounded 1,000-attempt wake. Legacy block/final-checksum tolerance is bounds-safe; cancellation after erase returns `Cancelled`. No hardware qualification is claimed. |
| FlashEcuSubaruHitachiSH7058Can | ECU | ISO-15765 | read, write | no | — | unqualified | — | Protocol `sub_ecu_hitachi_sh7058_can`; cfg `test_write=no`. |
| FlashEcuSubaruHitachiSH72543rCan | ECU | ISO-15765 | read, write | no | — | unqualified | — | Protocols `sub_ecu_hitachi_sh72543r_can` and `_can_recovery`; cfg `test_write=no` for both. |
| FlashEcuSubaruMitsuM32rKline | ECU | K-Line | read, write | yes | `subaru_mitsu_m32r_kline_plan_test`, `subaru_mitsu_m32r_kline_executor_test`, `test_flash_workflow`, `test_flash_dialog`, `test_protocols_cfg_subaru_mitsu_m32r_kline` @ Wave 1 | experimental | — | Portable `SubaruMitsuM32rKlinePlan`/executor owns exact protocol `sub_ecu_mitsu_m32r_kline` with `M32R_512KB_4blocks`. Traffic covers only `0x8000`-`0x80000` in 128-byte chunks; successful reads deliberately synthesize the unread boot region `0x0000`-`0x8000` as `0xFF`. `test_write` is rejected before I/O. Transfer buffers append 128 encrypted bytes safely instead of indexing beyond an empty container; mid-transfer cancellation returns `Cancelled` rather than the legacy success result. Missing or unexpected transfer acknowledgements retain legacy tolerance, while transport errors propagate. The common dialog deliberately uses its corrected ECU title and standardized completion/error messages. No hardware qualification is claimed. |
| FlashEcuSubaruUnisiaJecs | ECU | K-Line | read | no | — | unqualified | — | Protocols `sub_ecu_unisia_jecs_m3779x` and `_m3775x`; cfg `<mode>` is `SSM1` here (every other row in this table is `OBD2`). cfg read=yes, test_write=no, write=no — read-only family. |
| FlashEcuSubaruUnisiaJecsM32r | ECU | K-Line | varies by protocol variant — see notes | no | — | unqualified | — | Protocols `sub_ecu_unisia_jecs_20` and `_30` (cfg read/write=yes/yes) plus `_40` and `_70` (cfg read=yes, write=no) all dispatch to this one class; `test_write=no` throughout. |
| FlashTcuCvtSubaruHitachiM32rCan | TCU | ISO-15765 | read, write | yes | subaru_tcu_cvt_hitachi_m32r_can_plan_test, subaru_tcu_cvt_hitachi_m32r_can_executor_test @ Wave 3 | experimental | — | Protocol `sub_tcu_cvt_hitachi_m32r_can`; cfg `test_write=no`. Legacy `execute()` called the always-failing `hack_words()`, not `connect_bootloader()` — this family has never run in production. This port uses the real, previously-unreachable `connect_bootloader`/`read_mem`/`write_mem` logic instead, giving the family a working portable path for the first time; this is the wave's deliberate divergence. A second, related divergence: legacy `read_mem`'s `start_addr - 0x00100000` bias underflows to `0xFFF00000` for the only `start_addr` ever passed (0), bypassing its own `< 0x8000` floor-clamp — this port targets the clamp's evident intent (`{0x8000, 0x78000}`) rather than the never-observed underflowed address. A third divergence, found while porting: `read_mem`'s own stop-command check has an inverted comparison (`received.at(4) == 0x77` is treated as the error branch, opposite of every other close/stop check in the same file); reproducing it literally would make a successful read always report failure, so this exchange is routed through the same standard SID+0x40 matching used everywhere else in this file, which naturally yields the correct polarity without the bug. No hardware qualification is claimed. |
| FlashTcuCvtSubaruMitsuMH8104Can | TCU | ISO-15765 | read, write | yes | subaru_tcu_cvt_mitsu_mh8104_can_plan_test, subaru_tcu_cvt_mitsu_mh8104_can_executor_test @ Wave 3 | experimental | — | Protocol `sub_tcu_cvt_mitsu_mh8104_can`; cfg `test_write=no`. Every response check after the kernel-alive probe is commented out in legacy (`// return STATUS_ERROR;`) — this family tolerates any ECU response content and only a transport-level failure (timeout/disconnect/cancellation) stops it; preserved exactly, including the connect alive-recheck's `&&`-instead-of-`||` condition (line 334) which means that branch practically never fires for a well-formed response. No hardware qualification is claimed. |
| FlashTcuCvtSubaruMitsuMH8111Can | TCU | ISO-15765 | read, write | yes | subaru_tcu_cvt_mitsu_mh8111_can_plan_test, subaru_tcu_cvt_mitsu_mh8111_can_executor_test @ Wave 3 | experimental | — | Protocol `sub_tcu_cvt_mitsu_mh8111_can`; cfg `test_write=no`. Write targets block 3 (`{0x80000, 0x100000}`), the family's declared capacity's upper block; read is hardcoded to `{0x8000, 0x78000}` and does not overlap the write region — a genuine legacy asymmetry (read is an incomplete diagnostic dump, write assumes a full `0x180000`-byte user-supplied ROM image), preserved rather than reconciled. A second divergence, found while porting: legacy `erase_mem`'s mismatch branch has its `return STATUS_ERROR` commented out with no success branch anywhere, so `try_count` always reaches 20 and the function always returns failure — every write in this family has never worked in production. This port targets the evident intent (retry-until-match, matching the sibling close/checksum retry loops in the same file) instead of reproducing an always-failing erase step. No hardware qualification is claimed. |
| Subaru TCU Denso SH705x CAN | TCU | ISO-15765 | varies by protocol variant — see notes | yes | `subaru_tcu_denso_sh705x_can_plan_test`, `subaru_tcu_denso_sh705x_can_executor_test`, `test_denso_tcu_read_preflight`, `test_flash_workflow` @ Wave 5 | experimental | — | Exact portable routes only: `sub_tcu_denso_sh7055_can` supports read, while `sub_tcu_denso_sh7058_can` supports read and write; both reject test-write, SH7055 rejects write before transport I/O, and suffix/future variants remain unrouted. After positive kernel start, the executor performs the legacy reset → exact 0x7E1/0x7E9/500000/11-bit configure → open → cancellable 500 ms wait → strict kernel-ID probe sequence; every restart stage is fail-closed, while the caller-owned outer attempt still performs the final close exactly once. Reads preserve the four exact GUI choices (`Dump`, `Relearn`, `Read Param`, `Set Param`): Dump alone reaches `FlashDialog` and its single `Begin` ignition gate, while the three service choices retain their separate ignition gate and existing `ServiceFunctionDialog`; writes bypass the chooser. The initial kernel-ID probe maps no-frame, timeout, short, malformed, wrong-ID, wrong-opcode, and nonterminal adapter outcomes to legacy ECU initialization; cancellation and disconnection remain terminal, and post-upload verification remains strict. Every kernel-upload B6 block still performs one raw read, discarding no-frame, timeout, malformed, short, wrong-ID, negative, and nonterminal adapter outcomes; only cancellation/disconnection terminate it. Kernel start accepts service-only `0x71` or the full routine echo. **Automated-only session safety correction:** (1) oracle revision `59f4e442` line 230 uses `sid != 0x50 && subfunction != 0x03`, which accepts either partial match; (2) focused tests reproduce both `50 02` and `51 03`; (3) both are rejected while exact `50 03` proceeds to seed request; (4) this disclosure records the intentional strict `0x50 0x03` correction without advancing hardware status. **Task 5 Fix Round 1 four-artifact dynamic-log correction:** (1) the revision-`59f4e442` TCU oracle's `read_mem` declares but does not start its `QElapsedTimer` before the first sample at lines 634 and 731-736, while `flash_block` logs uninitialized `curspeed`/`tleft` at lines 1290-1293 and 1405-1414 before assigning them at lines 1421-1439; (2) `ZeroElapsedClockUsesOneMillisecondClampForExactReadAndWriteLogs` reproduces both zero-elapsed invariants with a deterministic `FakeClock`; (3) the portable executor samples `IClock::now_ms()` around every read page and write buffer and clamps zero elapsed time to 1 ms while retaining the legacy formulas and formats; (4) that test pins the exact corrected records at `1024000 B/s      1 s` and `512000 B/s, ~ 1 s`. **Wave 5 factoring closeout:** stock seed, standard index, and encrypt constants are shared only after the TCU's byte-exact suite passed unchanged; normal-ROM BEEF read payloads remain raw, and no Wave 5 ROM read consumes the common decrypt table. Qualification is automated-only: no bench or vehicle hardware qualification is claimed. |
| FlashTcuSubaruHitachiM32rCan | TCU | ISO-15765 | read, write | no | — | unqualified | — | Protocol `sub_tcu_hitachi_m32r_can`; cfg `test_write=no`. |
| FlashTcuSubaruHitachiM32rKline | TCU | K-Line | read | yes | `subaru_tcu_hitachi_m32r_kline_plan_test`, `subaru_tcu_hitachi_m32r_kline_executor_test`, `test_flash_workflow` | experimental | — | Protocol `sub_tcu_hitachi_m32r_kline`; cfg read=yes, test_write=no, write=no — read-only. Portable `SubaruTcuHitachiM32rKlinePlan`/executor. Four deliberate divergences from the legacy `FlashTcuSubaruHitachiM32rKlineOperation` (deleted this wave): (1) legacy `execute()`'s write branch only logged "Not yet implemented" and then returned `connect_bootloader()`'s `STATUS_SUCCESS`, reporting a successful write that wrote nothing — the portable plan rejects both `write` and `test_write` with `Unsupported` instead; (2) a block read that exhausts all five retry attempts now returns `BadResponse`, where legacy appended nothing for that block, continued anyway, and returned success, silently truncating the ROM; (3) block responses are now length-checked exactly (`block_len + 6`), where legacy accepted any response longer than 5 bytes and appended `length - 1` of it, misaligning every later byte; (4) six unreachable legacy functions were not ported: `read_b8`, `read_b0`, `read_a0_ram`, `send_sid_b0_block_write`, `send_sid_b8_byte_read`, `generate_can_seed_key`. **VERIFY:** the MCU binding `M32R_512KB` could not be confirmed from this repository — protocol-to-MCU mapping lives in EcuFlash-side definition files outside this repo. The only in-repo evidence is that the wave-3 TCU family uses the same value; treat this binding as needing external verification. No hardware qualification is claimed. |
| FlashEcuSubaruHitachiM32rJtag | JTAG | JTAG | read, test_write, write (inferred — see notes) | no | — | unqualified | — | **No corresponding `<protocol>` entry exists in `resources/shared/config/protocols.cfg`** — a case-insensitive search for "jtag" across the whole file returns zero matches. Yet `src/ui/desktop/mainwindow.cpp` (~line 1341) dispatches on `flash_protocol_selected_protocol_name.startsWith("sub_ecu_hitachi_m32r_jtag")` and instantiates `FlashEcuSubaruHitachiM32rJtag`, which wraps `FlashEcuSubaruHitachiM32rJtagOperation` (`src/platform/desktop/common/flash/legacy/jtag/flash_ecu_subaru_hitachi_m32r_jtag_operation.{h,cpp}`). This branch appears currently unreachable through the protocol dropdown since nothing in cfg can produce that protocol name; flagging explicitly per task instructions rather than omitting the row or guessing a cfg mapping. `operations` and `transport` here are inferred from the class's own members (`read_mem()`, `write_mem(bool test_write)`, a `test_write` flag, and JTAG IR/DR shift helpers) since there is no cfg entry to read capability flags from. |
| FlashEcuSubaruDensoMC68HC16Y5_02_BDM | BDM | BDM | read, write | no | — | unqualified | — | Protocol `sub_ecu_denso_mc68hc16y5_02_bdm`; cfg read=yes, test_write=no, write=yes. [README](../README.md)'s "Unbrick with FastECU" section lists this BDM path as "ROM implemented, TPU also coming" — a capability claim without a date, adapter identity, or operator, so it is not counted as `hardware_evidence` per this matrix's bar. |
| FlashEcuSubaruUnisiaJecsM32rBootMode | bootmode | K-Line | read, write | no | — | unqualified | — | Protocols `sub_ecu_unisia_jecs_20_bootmode` and `_30_bootmode`; cfg read=yes, test_write=no, write=yes for both. |
<!-- Append one row per remaining family found in resources/shared/config/protocols.cfg
     and src/platform/desktop/common/flash/legacy/{bdm,bootmode,ecu,jtag,tcu}/,
     each with portable=no, hardware_status=unqualified unless a specific
     historical report justifies otherwise. Do not invent family IDs --
     use the exact protocols.cfg <name> or the exact operation class name. -->

## Service functions

| family_id | scope | transport | operations | portable | automated_evidence | hardware_status | hardware_evidence | notes |
|---|---|---|---|---|---|---|---|---|
| TcuRelearn | TCU | ISO-15765 | relearn | yes | relearn_session_test, test_service_function_worker | experimental | — | unconditional `return STATUS_ERROR` at legacy `:786` removed so completion reports success; poll accepts `0xE8` rather than the unreachable `0xF8`; poll frame composed directly because legacy `:740-747` writes three bytes past the end of a 9-byte buffer. **VERIFY: the poll's terminal condition is unresolved — the 200-iteration bound is preserved and the last status frame is surfaced; a bench must establish which status value means complete.** |
| TcuReadParameters | TCU | ISO-15765 | read_parameters | yes | read_parameters_session_test, test_service_function_dialog | experimental | — | retry loop accepts `0xE8`, so the operation can succeed for the first time (legacy `:571-608` demanded `0xF8` then `0xE8`); response must be ≥15 bytes because the decode indexes byte 14 while legacy guarded `> 10`; results returned as data rather than log lines. |
| TcuSetParameters | TCU | K-Line | set_parameters | yes | tcu_parameter_table_test, set_parameters_session_test, test_service_function_dialog | experimental | — | all twelve frames composed from a table and framed once each. Legacy `:215` reassigned `output` to the framed array and `:237` onward mutated the SSM length byte and service ID, so only write 1 was well-formed and the operation aborted on write 2 with one correction applied and uncommitted. The two commit payloads are literally `B8 00 00 EC 55/AA`, so they target 24-bit address `0x0000ec`, distinct from the `0x0001xx` parameters. Each row is attempted once and silence/rejection stops immediately; repeated live or commit writes have no idempotency authority. **This is the largest behavior change in the work: the operation begins doing what its UI has always claimed. First bench item for this family.** |

### Enumeration notes

- `family_id` is the operation class name with its platform-specific suffix
  stripped (`...Operation` for the legacy `FlashOperationWorker` subclasses
  under `src/platform/desktop/common/flash/legacy/`, `...Executor` for the
  portable `src/backend/flash/eeprom/` classes) — the same convention the
  proving-pair rows already use. Several `protocols.cfg` `<protocol>` entries
  map to one family (e.g. five exact `*_densocan` protocol names resolve
  through the portable workflow); those are listed in the row's `notes`, not
  as separate rows, since the family — not the cfg alias — is the migratable
  unit.
- `resources/shared/config/protocols.cfg` has no top-level `<ECU>`/`<TCU>`/
  `<EEPROM>`/`<JTAG>`/`<BDM>`/`<bootmode>` XML sections; it is one flat
  `<protocols>` list of `<protocol name="...">` entries, followed by an
  unrelated `<car_models>` section that only *references* protocol names.
  This matrix's `scope` grouping is derived
  from each protocol's `<mode>`/`<ecu>`/`<flash_transport>` fields and the
  legacy directory (`bdm`/`bootmode`/`ecu`/`jtag`/`tcu`) that implements it,
  not from a cfg section tag.
- One cfg protocol, `mitsu_ecu_m32r_kline_mut_dma` (line 771), is excluded
  from this matrix entirely: it declares `read=no`, `test_write=no`,
  `write=no` and is not reachable through the flash dispatch table in
  `mainwindow.cpp` at all — it selects the MUT/DMA *logging* protocol
  (`src/backend/logging/protocols/portable_mut_dma_logging_protocol.*`), not
  a flash operation family, despite living in the same `<protocols>` list.
- Transport-column vocabulary mapping from cfg's free-text
  `<flash_transport>` values: cfg `CAN` (the proprietary DensoCAN bootloader
  framing) → `raw CAN`; cfg `iso15765,CAN` → `ISO-15765`; cfg `K-Line,CAN`
  (protocols selectable over either wire) → `K-Line, raw CAN`; cfg `K-Line`
  and `BDM` map through unchanged.
- Automated evidence for every row in this appended set is `—`: none of
  `src/platform/desktop/common/flash/legacy/{bdm,bootmode,ecu,jtag,tcu}/`
  has a `cc_test` target in its `BUILD.bazel` (verified by grep), unlike the
  portable EEPROM executors' proving-pair rows.

## Hardware qualification checklist

For any family being moved from `experimental`/`unqualified` toward `proven`:

- [ ] Adapter setup confirmed (correct cable, correct COM/USB port, correct protocol selected in the UI)
- [ ] Battery voltage/power stability confirmed stable for the full operation duration
- [ ] Read performed; SHA-256/hash of the read compared against a known-good reference where one exists
- [ ] Test-write performed (if the family supports it) and verified via a subsequent read/hash comparison
- [ ] Write performed (if the family supports it; `n/a` if not — never silently treated as passed)
- [ ] Post-write read/hash comparison performed
- [ ] Ignition cycle (power-cycle) performed between attempts per the family's documented sequence
- [ ] Recovery path exercised or confirmed available (e.g. boot-mode recovery, backup ROM restore)
- [ ] Logs captured and attached to the hardware evidence reference
- [ ] Operator and date recorded in the `hardware_evidence` column
