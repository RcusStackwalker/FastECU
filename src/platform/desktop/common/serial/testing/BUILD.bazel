load("//bazel:qt_targets.bzl", "COMMON_COPTS", "QT_DEPS", "fastecu_qttest", "qt_cc_library")

package(default_visibility = ["//src:__subpackages__"])

qt_cc_library(
    name = "fake_serial_backend",
    testonly = True,
    srcs = [],
    hdrs = ["fake_backend.h"],
    copts = COMMON_COPTS,
    deps = QT_DEPS + [
        # FakeBackend derives from SerialPortActionsDirect
        # (serial_port_actions_direct.h), which serial_platform_api's wrapped
        # target (serial_qt_compat) no longer carries after the direct/remote
        # backend split -- depend on :direct_serial_backend_for_tests, the
        # testonly alias for the platform's direct backend. fake_serial_backend
        # is itself testonly, and the binaries select their own platform
        # implementation.
        "//src/platform/desktop/common/serial:direct_serial_backend_for_tests",
        "//src/platform/desktop/common/serial:serial_platform_api",
        "@googletest//:gtest",
    ],
)

fastecu_qttest(
    name = "fake_backend_test",
    src = "fake_backend_test.cpp",
    deps = [
        ":fake_serial_backend",
        "//src/platform/desktop/common/serial:serial_platform_api",
    ],
)
