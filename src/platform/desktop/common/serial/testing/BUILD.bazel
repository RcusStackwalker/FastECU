load("//bazel:qt_targets.bzl", "COMMON_COPTS", "QT_DEPS", "qt_cc_library")

package(default_visibility = ["//src:__subpackages__"])

qt_cc_library(
    name = "fake_serial_backend",
    testonly = True,
    srcs = [],
    hdrs = ["fake_backend.h"],
    copts = COMMON_COPTS,
    deps = QT_DEPS + ["//src/platform/desktop/common/serial:serial_platform_api"],
)
