load("@rules_cc//cc:cc_library.bzl", "cc_library")
load("@rules_cc//cc:cc_test.bzl", "cc_test")
load("//bazel:qt_targets.bzl", "COMMON_COPTS", "QT_DEPS", "fastecu_qttest")

package(default_visibility = [
    "//src/algorithms:__subpackages__",
    "//src/backend:__subpackages__",
    "//src/platform:__subpackages__",
    "//src/ui:__subpackages__",
    "//tests:__pkg__",
])

# root //:portable_closure reads this file as `data` to check that portable
# algorithms targets stay Qt-free; that reference is plumbing, not a layer
# dependency, so export it to the root package specifically rather than
# widen the package's general default_visibility.
exports_files(
    ["BUILD.bazel"],
    visibility = ["//:__pkg__"],
)

cc_library(
    name = "menu",
    srcs = ["menu_command.cpp"],
    hdrs = ["menu_command.h"],
)

# TRANSITIONAL Qt shim: QString overloads delegating to :menu.
cc_library(
    name = "qt_compat",
    hdrs = ["qt_menu_command.h"],
    copts = COMMON_COPTS,
    deps = QT_DEPS + [":menu"],
)

cc_test(
    name = "menu_command_test",
    size = "small",
    srcs = ["menu_command_test.cpp"],
    deps = [
        ":menu",
        "@googletest//:gtest_main",
    ],
)

fastecu_qttest(
    name = "test_menu_command",
    src = "menu_command_qt_compat_test.cpp",
    deps = [":qt_compat"],
)

filegroup(
    name = "test_layout_cpp_sources",
    testonly = True,
    srcs = glob(["*test*.cpp"]),
    visibility = ["//:__pkg__"],
)
